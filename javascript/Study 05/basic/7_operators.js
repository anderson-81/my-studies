/*
Operadores aritméticos
		Operadores de comparação
		Operadores lógicos (ou relacionais)
		Operadores bit a bit (ESTUDAR MELHOR)
		Operadores de atribuição
		Operadores condicionais (ou ternários)
		Operador diverso
			operador condicional (? :);
			operador typeof => O operador typeof é um operador unário que é colocado antes de seu único operando, que pode ser de qualquer tipo. Seu valor é uma cadeia que indica o tipo de dados do operando.
			
				Type	String Returned by typeof
				Number		"number"
				String		"string"
				Boolean		"boolean"
				Object		"object"
				Function	"function"
				Undefined	"undefined"
				Null		"object"
*/

var num = 30;
console.log(num >= 0 ? true : false);

/*--------------------------*/


console.log("typeof: " + typeof(num));