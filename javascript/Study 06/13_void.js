// Funções void:

/*
Quando usamos uma Chamada imediata das expressões da função, valores nulos 
podem ser usado para forçar a palavra-chave  da função a ser tratada como 
uma expressão em vez de uma declaração.
*/

void function greeting(name){
    console.log(`Hello, ${name}!`);
}("Anderson");