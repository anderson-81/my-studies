select * from city join state on city.STATE_ID = state.ID;

select * from city left join state on city.STATE_ID = state.ID;

select * from city right join state on city.STATE_ID = state.ID;

select * from city full join state on city.STATE_ID = state.ID;

select * from city inner join state on city.STATE_ID = state.ID;